function add(a, b) {
    let sum = 0;
    return sum = a + b;
}

function subtract(a, b) {
    let sum = 0;
    return sum = a - b;
}

function multiply(a, b) {
    let sum = 0;
    return sum = a * b;
}

function divide(a, b) {
    let sum = 0;
    return sum = a / b;
}

// operate function
